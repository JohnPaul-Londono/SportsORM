<div class="card">
    <div class="card-header bg-dark text-light">Level 1</div>
    <div class="card-body">
        <p>Using the SportsORM, complete all the following queries and show their results on Level1.cshtml.</p>
        <ol>
            <li>...all womens' leagues</li>
            <table class="table table-striped">
                <tr>
                    <th>League Name</th>
                </tr>
                @foreach (var w in ViewBag.AllWomenLeague)
                {
                    <tr>
                        <td>@w.Name</td>
                    </tr>
                }
            </table>

            <li>...all leagues where sport is any type of hockey</li>
            <table class="table table-striped">
                <tr>
                    <th>League Name</th>
                </tr>
                @foreach (var h in ViewBag.AnyHockey)
                {
                    <tr>
                        <td>@h.Name , @h.Sport</td>
                    </tr>
                }
            </table>

            <li>...all leagues where sport is something OTHER THAN football</li>
            <table class="table table-striped">
                <tr>
                    <th>League Name</th>
                </tr>
                @foreach (var noFoot in ViewBag.NoFootball)
                {
                    <tr>
                        <td>@noFoot.Name </td>
                    </tr>
                }
            </table>

            <li>...all leagues that call themselves "conferences"</li>
            <table class="table table-striped">
                <tr>
                    <th>League Name</th>
                </tr>
                @foreach (var sc in ViewBag.SelfConference)
                {
                    <tr>
                        <td>@sc.Name </td>
                    </tr>
                }
            </table>

            <li>...all leagues in the Atlantic region</li>
            <table class="table table-striped">
                <tr>
                    <th>League Name</th>
                </tr>
                @foreach (var sc in ViewBag.AtlanLoc)
                {
                    <tr>
                        <td>@sc.Name</td>
                    </tr>
                }
            </table>

            <li>...all teams based in Dallas</li>
            <table class="table table-striped">
                <tr>
                    <th>Teams</th>
                </tr>
                @foreach (var dt in ViewBag.Dallas)
                {
                    <tr>
                        <td>@dt.TeamName</td>
                    </tr>
                }
            </table>
            <li>...all teams named the Raptors</li>
            <table class="table table-striped">
                <tr>
                    <th>Teams</th>
                </tr>
                @foreach (var rap in ViewBag.Raptors)
                {
                    <tr>
                        <td>@rap.Location @rap.TeamName </td>
                    </tr>
                }
            </table>
            <li>...all teams whose location includes "City"</li>
            <table class="table table-striped">
                <tr>
                    <th>Teams</th>
                </tr>
                @foreach (var city in ViewBag.City)
                {
                    <tr>
                        <td>@city.Location @city.TeamName </td>
                    </tr>
                }
            </table>

            <li>...all teams whose names begin with "T"</li>
            <table class="table table-striped">
                <tr>
                    <th>Teams</th>
                </tr>
                @foreach (var t in ViewBag.teamT)
                {
                    <tr>
                        <td>@t.Location @t.TeamName </td>
                    </tr>
                }
            </table>
            <li>...all teams, ordered alphabetically by location</li>
            <table class="table table-striped">
                <tr>
                    <th>Teams</th>
                </tr>
                @foreach (var alp in ViewBag.Alpha)
                {
                    <tr>
                        <td>@alp.Location @alp.TeamName </td>
                    </tr>
                }
            </table>
            <li>...all teams, ordered by team name in reverse alphabetical order</li>
            <table class="table table-striped">
                <tr>
                    <th>Teams</th>
                </tr>
                @foreach (var rev in ViewBag.reverseCity)
                {
                    <tr>
                        <td>@rev.Location @rev.TeamName </td>
                    </tr>
                }
            </table>
            <li>...every player with last name "Cooper"</li>
            <table class="table table-striped">
                <tr>
                    <th>Players</th>
                </tr>
                @foreach (var coop in ViewBag.Cooper)
                {
                    <tr>
                        <td>@coop.FirstName @coop.LastName </td>
                    </tr>
                }
            </table>
            <li>...every player with first name "Joshua"</li>
            <table class="table table-striped">
                <tr>
                    <th>Players</th>
                </tr>
                @foreach (var jo in ViewBag.Josh)
                {
                    <tr>
                        <td>@jo.FirstName @jo.LastName </td>
                    </tr>
                }
            </table>
            <li>...every player with last name "Cooper" EXCEPT those with "Joshua" as the first name</li>
            <table class="table table-striped">
                <tr>
                    <th>Players</th>
                </tr>
                @foreach (var njo in ViewBag.NoJosh)
                {
                    <tr>
                        <td>@njo.FirstName @njo.LastName </td>
                    </tr>
                }
            </table>
            <li>...all players with first name "Alexander" OR first name "Wyatt"</li>
            <table class="table table-striped">
                <tr>
                    <th>Players</th>
                </tr>
                @foreach (var awo in ViewBag.AlexOrWyatt)
                {
                    <tr>
                        <td>@awo.FirstName @awo.LastName </td>
                    </tr>
                }
            </table>
        </ol>
    </div>
</div>